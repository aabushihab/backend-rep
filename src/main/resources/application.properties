#spring.application.name=doctor-app-backend
## PostgreSQL configuration
#spring.datasource.url=jdbc:postgresql://localhost:5432/clinicdb
#spring.datasource.username=postgres
#spring.datasource.password=sgbj@2022
#spring.datasource.driver-class-name=org.postgresql.Driver
#
## Hibernate settings
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
## Use specific schema
#
#logging.level.org.springframework.web=DEBUG
##spring.jpa.properties.hibernate.default_schema=clinicdb
#
## Server port (optional, default 8080)
#server.port=8082
## Will Create Setup Screen
#clinic.open.hour=09:00
#clinic.close.hour=20:00
##Appointments Time to book it
#schedule.buffer.minutes=10
#
#
#
## Database Backup Configuration
#backup.db-name=clinicdb
#backup.db-user=postgres
#backup.db-password=sgbj@2022
#backup.dir=C:/backups
#backup.cron=0 0,39 2 * * ?
#above for local

#Below for Cloud :


spring.application.name=doctor-app-backend

# PostgreSQL configuration using Railway's actual env vars
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Server port
server.port=${PORT:8080}

# Clinic settings
clinic.open.hour=09:00
clinic.close.hour=20:00
schedule.buffer.minutes=10

# Backup configuration
backup.enabled=false